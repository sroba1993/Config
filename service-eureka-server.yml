server:
  port: 8762

configuration:
  text: Configurando ambiente por defecto  

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

logging:
  level:
    com:
      netflix:
        eureka: OFF
        discovery: OFF