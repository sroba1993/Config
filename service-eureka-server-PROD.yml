server:
  port: 8761

configuration:
  text: Configurando ambiente de produccion

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

logging:
  level:
    com:
      netflix:
        eureka: OFF
        discovery: OFF